// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyBSrCkSQaccO2x06Oua_3mVwquQIKZP8UM',
    authDomain: 'send-to-inbox-app.firebaseapp.com',
    databaseURL: 'https://send-to-inbox-app.firebaseio.com',
    projectId: 'send-to-inbox-app',
    storageBucket: 'send-to-inbox-app.appspot.com',
    messagingSenderId: '68444599377',
    appId: '1:68444599377:web:52f4829067ee27a0823470',
    measurementId: 'G-SH3KP10HV9'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
